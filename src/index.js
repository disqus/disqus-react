import { CommentCount } from './CommentCount';
import { CommentEmbed } from './CommentEmbed';

export { CommentCount };
export { CommentEmbed };

const Disqus = {
    CommentCount,
    CommentEmbed,
};

export default Disqus;
